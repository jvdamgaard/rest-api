module.exports = function(req, res, next) {

    // var resource = req.params.resource;
    // var user = req.user;
    // var method = req.method;
    // TODO: check if user has authorazation to method on resource
    next();
};
